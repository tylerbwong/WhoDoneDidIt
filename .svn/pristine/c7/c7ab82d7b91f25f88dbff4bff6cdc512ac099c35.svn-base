package whodonedidit.version1_0;

import java.util.ArrayList;
import java.util.List;
import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import static org.junit.Assert.*;

/**
 *
 * @author brittanyberlanga
 * @since 11-20-15
 * @version 1.0
 */
public class GetNotifiedTest
{
    public GetNotifiedTest()
    {
    }
    
    @BeforeClass
    public static void setUpClass()
    {
    }
    
    @AfterClass
    public static void tearDownClass()
    {
    }
    
    @Before
    public void setUp()
    {
    }
    
    @After
    public void tearDown()
    {
    }

    /**
     * Test of doInstruction method, of class GetNotified.
     */
    @Test
    public void testDoInstruction()
    {
        System.out.println("doGetNotified");
        String notification = "notification";
        Player player = new Player(0, "player");
        List<Player> players = new ArrayList<>();
        players.add(player);
        //player.setTable(new Table(players));
        //GetNotified instruction = new GetNotified(notification);
        //instruction.doInstruction(player);
        assertEquals(player.getPlayerID() == 0, true);
        //assertEquals(player.getTable().getLastNotification().equals(notification), true);
    }
}